import React, {useState,} from "react";
import axios from 'axios';

function Home(props){
const [data, setData] = useState(null);

    return(
        <div>
            hi
        </div>
    )
}

export default Home;